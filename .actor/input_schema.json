{
    "title": "Daily Power Intelligence Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "debugMode": {
            "title": "Debug Mode",
            "type": "boolean",
            "description": "Enable verbose logging.",
            "default": false
        },
        "timeoutSecs": {
            "title": "Request Timeout",
            "type": "integer",
            "description": "Timeout for Eskom API requests in seconds.",
            "default": 30,
            "minimum": 1,
            "maximum": 300
        }
    }
}